<%= form_with model: @task, url: tasks_path, local: true do |f| %>
<div>
  タスク名<%= f.text_field :task_title %>
</div>
<div>
  メモ<%= f.text_area :memo %>
</div>
<div>
予定日<%= f.date_field :limit_date %>
</div>
<div>
<%= f.submit :submit %>
</div>
<% end %>